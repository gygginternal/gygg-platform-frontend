import styles from './GigHistoryPage.module.css';

const GigHistoryPage = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>Gig History</h1>
      <p className={styles.message}>This page will display the history of gigs.</p>
      {/* Additional content can be added here */}
    </div>
  );
};
